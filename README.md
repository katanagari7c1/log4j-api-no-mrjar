# Log4j2 patch for non Multi-release Jar (MRJAR) support

To build and create the jar file run the following command:
```
./build-jar.sh
```

Resulting jar will be created as `output/log4j-api-no-mrjar-2.11.1.jar`.